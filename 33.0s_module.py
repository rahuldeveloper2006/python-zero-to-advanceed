#the full form of os module is operating system module
#it is a built in module library in python
#if we create directory then we use
# import os
# if(not os.path.exists("puja")):
#     os.mkdir("puja")
# # i=0
# for i in range(5+1):
#     os.mkdir(f"puja/day{i}")
# if we want remove the directory
#if we want to rename the all creat folder ,then
# for i in range(5+1):
#     os.rename(f"puja/rahul{i+1}",f"puja/tutorial{i+1}")
#if we want to see all folders, then
# folders=os.listdir("puja")
# for folder in folders:
#     print(folder)
#if we want to see all files in folders
#then we use:-
# folders=os.listdir("puja")
# for folder in folders:
#     print(folder)
#     print(os.listdir(f"puja/{folder}"))
